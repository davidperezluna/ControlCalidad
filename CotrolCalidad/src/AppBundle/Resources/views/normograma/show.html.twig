{% extends 'base.html.twig' %}

{% block body %}
    <h1>Normograma</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ normograma.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('normograma_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('normograma_edit', { 'id': normograma.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
