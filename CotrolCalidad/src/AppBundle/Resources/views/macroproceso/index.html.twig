{% extends 'base.html.twig' %}

{% block referencia %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li class="active">Macro Proceso</li>
    </ol>
{% endblock %} 

{% block body %}
     <div class="col-lg-12">
    <div class="col-lg-1">
                    <a href="{{ path('macroproceso_new')}}" class="btn btn-app btn-xs">
                        <i class="fa fa-plus"></i> Agregar
                    </a>
                </div>
       
        <div class="col-lg-12">
          <div class="box">
            <div class="box-header">
                
              <h3 class="box-title">Macro Proceso</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">

                <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Actions</th>
                    </tr>
                </thead>

                <tbody>
                {% for macroProceso in macroProcesos %}
                        <td><a href="{{ path('macroproceso_show', { 'id': macroProceso.id }) }}">{{ macroProceso.id }}</a></td>
                        <td>{{ macroProceso.nombre }}</td>
                        <td>
                            <div class="btn-group">
                              <a href="{{ path('macroproceso_edit', { 'id': macroProceso.id }) }}" class="btn btn-default btn-flat"><i class="fa fa-edit"></i></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
               
              </table>

            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
          </div>
           
</div>
{% endblock %}
 