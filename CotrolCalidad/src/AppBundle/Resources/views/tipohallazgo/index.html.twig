{% extends 'base.html.twig' %}

{% block referencia %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li class="active">Tipo Documento</li>
    </ol>
{% endblock %} 

{% block body %}
     <div class="col-lg-12">
    <div class="col-lg-1">
                    <a data-toggle="tooltip"data-original-title="Agregar nuevo tipo de hallazgo" href="{{ path('tipohallazgo_new')}}" class="btn btn-app btn-xs">
                        <i class="fa fa-plus"></i> Agregar
                    </a>
                </div>
       
        <div class="col-lg-12">
          <div class="box">
            <div class="box-header">
                
              <h3 class="box-title">Tipo Docuemnto</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">

                <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Plazo</th>
                    </tr>
                </thead>

                <tbody>
               {% for tipoHallazgo in tipoHallazgos %}
                    <tr>
                        <td>{{ tipoHallazgo.nombre }}</td>
                        <td>{{ tipoHallazgo.plazo }}</td>
                    </tr>
                {% endfor %}
                </tbody>
              </table>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
          </div>
</div>
{% endblock %}
