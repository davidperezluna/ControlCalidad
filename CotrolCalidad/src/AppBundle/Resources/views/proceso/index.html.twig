{% extends 'base.html.twig' %}

{% block referencia %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li class="active">Proceso</li>
    </ol>
{% endblock %} 

{% block body %}
    <div class="col-lg-12">
    <div class="col-lg-1">
                    <a href="{{ path('proceso_new')}}" class="btn btn-app btn-xs">
                        <i class="fa fa-plus"></i> Agregar
                    </a>
                </div>
       
        <div class="col-lg-12">
          <div class="box">
            <div class="box-header">
                
              <h3 class="box-title">Empresa</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">

                <thead>
                <tr>
                    <th>Id</th>
                    <th>Codigo</th>
                    <th>Nombre</th>
                    <th>Version</th>
                    <th>Vigencia</th>
                    <th>Actions</th>
                </tr>
                </thead>

                <tbody>
                {% for proceso in procesos %}
                    <tr>
                        <td><a href="{{ path('proceso_show', { 'id': proceso.id }) }}">{{ proceso.id }}</a></td>
                        <td>{{ proceso.codigo }}</td>
                        <td>{{ proceso.nombre }}</td>
                        <td>{{ proceso.version }}</td>
                        <td>{% if proceso.vigencia %}{{ proceso.vigencia|date('Y-m-d') }}{% endif %}</td>
                        <td>
                            <div class="btn-group">
                              <a href="{{ path('proceso_edit', { 'id': proceso.id }) }}" class="btn btn-default btn-flat"><i class="fa fa-edit"></i></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
               
              </table>

            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
          </div>
           
</div>
{% endblock %}
