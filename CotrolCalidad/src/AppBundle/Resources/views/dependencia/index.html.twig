{% extends 'base.html.twig' %}

{% block referencia %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li class="active">Dependencia</li>
    </ol>
{% endblock %}

{% block body %}
<!-- /.box -->
<div class="col-lg-12">
        <div class="col-lg-1">
            <a href="{{ path('dependencia_new')}}" class="btn btn-app btn-xs">
                <i class="fa fa-plus"></i> Agregar
            </a>
        </div>
       
        <div class="col-lg-12">
          <div class="box">
            <div class="box-header">
                
              <h3 class="box-title">Dependencia</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">

                <thead>
                <tr>
                    <th>Id</th>
                    <th>Nit</th>
                    <th>Nombre</th>
                    <th>Representante</th>
                    <th>Actions</th>
                </tr>
                </thead>

                <tbody>
                 {% for dependencium in dependencias %}
                    <tr>
                        <td><a href="{{ path('dependencia_show', { 'id': dependencium.id }) }}">{{ dependencium.id }}</a></td>
                        <td>{{ dependencium.nit }}</td>
                        <td>{{ dependencium.nombre }}</td>
                        <td>{{ dependencium.representante }}</td>
                        <td>
                            <div class="btn-group">
                              <a href="{{ path('dependencia_show', { 'id': dependencium.id }) }}" class="btn btn-default btn-flat"><i class="fa fa-eye"></i></a>
                              <a href="{{ path('dependencia_edit', { 'id': dependencium.id }) }}" class="btn btn-default btn-flat"><i class="fa fa-edit"></i></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
               
              </table>

            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
      </div>
           
</div>
{% endblock %}
