{% extends 'base.html.twig' %}

{% block body %}
    <h1>Archivos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Version</th>
                <th>Descripcion</th>
                <th>Estado</th>
                <th>Urldocumento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for archivo in archivos %}
            <tr>
                <td><a href="{{ path('archivo_show', { 'id': archivo.id }) }}">{{ archivo.id }}</a></td>
                <td>{{ archivo.version }}</td>
                <td>{{ archivo.descripcion }}</td>
                <td>{% if archivo.estado %}Yes{% else %}No{% endif %}</td>
                <td>{{ archivo.urlDocumento }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('archivo_show', { 'id': archivo.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('archivo_edit', { 'id': archivo.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('archivo_new') }}">Create a new archivo</a>
        </li>
    </ul>
{% endblock %}
