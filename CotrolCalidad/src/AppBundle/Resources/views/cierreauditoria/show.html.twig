{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cierreauditorium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cierreAuditorium.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{% if cierreAuditorium.fecha %}{{ cierreAuditorium.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cierreAuditorium.estado }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ cierreAuditorium.observaciones }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cierreauditoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cierreauditoria_edit', { 'id': cierreAuditorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
