{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-4">
    <h1>Editar Cierre</h1>

    {{ form_start(edit_form) }}
        <label>Usuario:</label>
        {{ form_widget(edit_form.usuario, {'attr': {'class': 'form-control select2'}}) }}
        <label>Estado:</label>
        {{ form_widget(edit_form.estado, {'attr': {'class': 'form-control select2'}}) }}
        <br>
        <br>
        <input type="submit" class="btn btn-default btn-block" value="Editar" />
    {{ form_end(edit_form) }}
    </div>

{% endblock %}

