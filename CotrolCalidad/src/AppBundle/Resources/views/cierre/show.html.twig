{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cierre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cierre.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cierre.estado }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cierre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cierre_edit', { 'id': cierre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
