{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-4">
    <h1>Asignar Cierre</h1>

    {{ form_start(form) }}
        <label>Usuario:</label>
        {{ form_widget(form.usuario, {'attr': {'class': 'form-control select2'}}) }}
        <label>Estado:</label>
        {{ form_widget(form.estado, {'attr': {'class': 'form-control select2'}}) }}
        <br>
        <br>
        <input type="submit" class="btn btn-default btn-block" value="Asignar" />
    {{ form_end(form) }}
    </div>

{% endblock %}
