{% extends 'base.html.twig' %}

{% block body %}
    <h1>Documentolegal</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ documentoLegal.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ documentoLegal.nombre }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ documentoLegal.url }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('documentolegal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('documentolegal_edit', { 'id': documentoLegal.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
