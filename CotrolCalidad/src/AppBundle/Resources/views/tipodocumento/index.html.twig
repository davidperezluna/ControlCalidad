{% extends 'base.html.twig' %}

{% block referencia %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li class="active">Tipo Documento</li>
    </ol>
{% endblock %} 

{% block body %}
     <div class="col-lg-12">
    <div class="col-lg-1">
                    <a data-toggle="tooltip"data-original-title="Agregar nuevo tipo de documento" href="{{ path('tipodocumento_new')}}" class="btn btn-app btn-xs">
                        <i class="fa fa-plus"></i> Agregar
                    </a>
                </div>
       
        <div class="col-lg-12">
          <div class="box">
            <div class="box-header">
                
              <h3 class="box-title">Tipo Docuemnto</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">

                <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Sigla</th>
                    <th>Actions</th>
                    </tr>
                </thead>

                <tbody>
               {% for tipoDocumento in tipoDocumentos %}
                    <tr>
                        <td><a href="{{ path('tipodocumento_show', { 'id': tipoDocumento.id }) }}">{{ tipoDocumento.id }}</a></td>
                        <td>{{ tipoDocumento.nombre }}</td>
                        <td>{{ tipoDocumento.sigla }}</td>
                        <td>
                            <div class="btn-group">
                              <a data-toggle="tooltip"data-original-title="editar" href="{{ path('tipodocumento_edit', { 'id': tipoDocumento.id }) }}" class="btn btn-default btn-flat"><i class="fa fa-edit"></i></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
               
              </table>

            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
          </div>
           
</div>
{% endblock %}
