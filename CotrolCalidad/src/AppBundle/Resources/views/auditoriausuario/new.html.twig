{% extends 'base.html.twig' %}

{% block referencia %}
     <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li><a href="{{ path('auditoria_show', { 'id': idAuditoria }) }}">Usuarios</a></li>
        <li class="active">Nuevo</li>
      </ol>
{% endblock %}

{% block body %}
    <h1>Auditoriausuario creation</h1>

    <div class="col-lg-5">
                  <a href="{{ path('auditoria_show', { 'id': idAuditoria }) }}" style="margin-left: 89%;" class="link-black text-sm col-lg-offset-10"><i class="fa fa-mail-reply margin-r-5"></i> Atras</a>

        <div class="box box-success col-lg-5 ">
                <div class="box-header with-border">
                  <h3 class="box-title">Crear Entidad</h3>
                </div>
                 {{ form_start(form) }}
                    <br>
                    <br>
                     <label>Role Responsable</label>
                    {{ form_widget(form.usuario, {'attr': {'class': 'form-control select2'}}) }}
                    {{ form_widget(form) }} 
                    <div class="box-footer">
                    <button type="submit" class="btn btn-primary">Crear</button>
                  </div>
                {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
