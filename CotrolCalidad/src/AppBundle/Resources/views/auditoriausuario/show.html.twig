{% extends 'base.html.twig' %}

{% block body %}
    <h1>Auditoriausuario</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auditoriaUsuario.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ auditoriaUsuario.role }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('auditoriausuario_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('auditoriausuario_edit', { 'id': auditoriaUsuario.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
