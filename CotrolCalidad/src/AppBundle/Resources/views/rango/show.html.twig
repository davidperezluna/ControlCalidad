{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rango</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rango.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ rango.nombre }}</td>
            </tr>
            <tr>
                <th>Numeroniveles</th>
                <td>{{ rango.numeroNiveles }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rango_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rango_edit', { 'id': rango.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
