{% extends 'base.html.twig' %}
{% block referencia %}
     <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li><a href="{{ path('homepage')}}">Proceso Usuario</a></li>
        <li class="active">Nuevo</li>
      </ol>
{% endblock %}
{% block body %}
    <div class="col-lg-5">
    <div class="col-lg-10">
                  <a href="{{ path('cargo_index') }}" style="margin-left: 89%;" class="link-black text-sm col-lg-offset-10"><i class="fa fa-mail-reply margin-r-5"></i> Atras</a>
        <div class="box box-success col-lg-5 ">
                <div class="box-header with-border">
                  <h3 class="box-title">Asignar Persona</h3>
                </div>

                 {{ form_start(form) }}
                    <label>Persona</label>
                    {{ form_widget(form.usuario, {'attr': {'class': 'form-control select2'}}) }}
                    {{ form_widget(form.proceso, {'attr': {'class': ' hidden'}}) }}
                    <label>Role</label>
                    {{ form_widget(form.role, {'attr': {'class': 'form-control select2'}}) }}

                     <br>
                    <div class="box-footer">
                    <button type="submit" class="btn btn-default btn-block">Asignar</button>
                  </div>
                    {{ form_end(form) }}

                
                <br>
        </div>
    </div>
    <div class="col-lg-2">
    <a href="{{ path('usuario_interno_new',{'idProceso':proceso.id}) }}" class="btn btn-app">
                    <i class="fa fa-user"></i> <small>Crear Usuario</small>
    </a>
    </div>
    </div>
{% endblock %}
